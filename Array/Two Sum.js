/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number[]}
 */
var twoSum = function(nums, target) {
    let map ={};
    for(let i=0;i<nums.length;i++){
       map[input[i]] = i;
    }
};

nums = twoSum([2, 7, 11, 15],9);
console.log(nums);